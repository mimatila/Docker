Harjoitus 3.3 idea:

Ao. ssh scriptillä(./mika.sh) kloonataan repo lokaali levylle ja buildataan image joka siirretään DockerHubiin.

#/bin/bash
repository="git@github.com:mimatila/docker-frontend-youtube-3.3.git"
localFolder="/home/mimatila/Docker/3.4"
git clone "$repository" "$localFolder"
cd 3.4
sudo docker build -t frontend_ssh .
sudo docker tag frontend_ssh:latest mimatila/frontend_ssh
sudo docker login
sudo docker push mimatila/frontend_ssh

Tämän jälkeen image ajetaan seuraavalla komennolla:

docker run -ti /3.3/mimatila/frontend_ssh /mika.sh abc
